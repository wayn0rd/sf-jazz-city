/// <reference types="vitest/globals" />

// Registers @testing-library/jest-dom's matchers on Vitest's `expect`,
// and augments the Vitest matcher types (spec.md C10).
import '@testing-library/jest-dom/vitest';
